if name == 'mnist':
#     model.fit(x=x_train,y=x_train, epochs=1, batch_size=600, callbacks=callback_list)
# elif name == 'cifar10':
#     model.fit(x=x_train,y=x_train, epochs=20, batch_size=500, callbacks=callback_list)